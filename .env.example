# Base URL of the backend API (leave unset to use same-origin /api)
VITE_API_URL=https://api.centre-hassan2.ma/api
# Target of the Vite dev proxy for /api requests (default http://127.0.0.1:8000)
VITE_API_PROXY=http://127.0.0.1:8000
